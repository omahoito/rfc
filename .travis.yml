# assumption: Travis build runs in bash 4.0 (or greater)

before_script:
  - chmod +x *.sh
  - sudo pip install json-spec

script:
  - ./xmlvalidator.sh
  - ./jsonvalidator.sh

notifications:
  email: false
